export default interface frontMatter {
  coverImage?: string;
  category: string;
  date: string;
  description: string;
  tags: string[];
  title: string;
  githubLink?: string;
  demoLink?: string;
}
